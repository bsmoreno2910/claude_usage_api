version: "3.9"

services:
  claude-usage-api:
    build: .
    container_name: claude-usage-api
    ports:
      - "8000:8000"
    environment:
      API_KEY: "troque-essa-chave"
      PROFILES_BASE: "/data/profiles"
      CLAUDE_BIN: "claude"
      REQUEST_TIMEOUT: "120"
    volumes:
      - claude_profiles:/data/profiles
    restart: unless-stopped

volumes:
  claude_profiles:
